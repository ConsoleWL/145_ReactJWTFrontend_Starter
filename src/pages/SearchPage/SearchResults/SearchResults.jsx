const searchResults = ({ searchResults }) => {
  //  let bookItems = searchResults.map((book) <div> book.Id, book.text. book.rating</div>)
  return (
    <div>
      <p>Search result container</p>
      {/* <div>{bookItems}</div> */}
    </div>
  );
};

export default searchResults;
